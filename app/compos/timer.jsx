var React = require("react"),
$ = require("jquery")

var Timer = React.createClass({
    
    
    render: function(){
        //console.log("$", $)
        return (
            <h2>Timer HERE</h2>
            
            )
    }
})

module.exports = Timer